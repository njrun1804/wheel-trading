# === CLAUDE INTEGRATION REQUIREMENTS ===
# Specific dependencies for Claude Code integration

# === ANTHROPIC API ===
anthropic>=0.40.0,<1.0

# === CORE DEPENDENCIES ===
numpy>=1.24.0,<2.0
aiofiles>=23.0.0,<25.0
pydantic>=2.0.0,<3.0

# === ASYNC MESSAGING (optional) ===
asyncio-mqtt>=0.13.0,<1.0

# === APPLE SILICON OPTIMIZATION ===
mlx>=0.15.0,<1.0; sys_platform == "darwin" and platform_machine == "arm64"

# === MCP INTEGRATION ===
mcp>=1.0.0,<2.0
langchain-mcp-adapters>=0.1.0,<1.0