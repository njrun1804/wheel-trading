# === BOLT 8-AGENT SYSTEM REQUIREMENTS ===
# Hardware-accelerated multi-agent orchestration

# === CORE FRAMEWORK ===
click>=8.0.0,<9.0
psutil>=5.8.0,<8.0
rich>=10.0.0,<15.0
pydantic>=2.0.0,<3.0
tenacity>=8.0.0,<10.0
requests>=2.32.0,<3.0

# === NUMERICAL COMPUTING ===
numpy>=1.21.0,<2.0
pandas>=1.3.0,<3.0
scipy>=1.7.0,<2.0

# === ASYNC & I/O ===
aiofiles>=0.7.0,<25.0
asyncio-mqtt>=0.13.0,<1.0
uvloop>=0.17.0,<1.0; sys_platform != "win32"

# === PERFORMANCE & STORAGE ===
orjson>=3.8.0,<4.0
lmdb>=1.4.0,<2.0

# === APPLE SILICON ACCELERATION (MLX-FIRST) ===
mlx>=0.0.1,<1.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.0.1,<1.0; sys_platform == "darwin" and platform_machine == "arm64"
# torch>=2.0.0,<3.0; sys_platform == "darwin" and platform_machine == "arm64"  # DISABLED - PyTorch fallback only

# === HARDWARE MONITORING (Apple Silicon) ===
# pynvml>=11.4.0,<13.0  # REMOVED - NVIDIA GPU monitoring not needed on M4 Pro
psutil>=5.8.0,<8.0  # Already included above for system monitoring

# === SECURITY & UTILITIES ===
cryptography>=3.4.0,<46.0
python-dateutil>=2.8.0,<3.0
pytz>=2022.1,<2026.0

# === DEVELOPMENT TOOLS (optional) ===
pytest>=7.0.0,<9.0
pytest-asyncio>=0.21.0,<2.0
black>=22.0.0,<25.0
mypy>=1.0.0,<2.0

# === PERFORMANCE PROFILING ===
py-spy>=0.3.0,<1.0
memory-profiler>=0.60.0,<1.0